{
  "name": "jquery-jsonrpcclient",
  "version": "0.4.0",
  "description": "JSON-RPC 2.0 Client for HTTP and WebSocket backend",
  "main": "jquery.jsonrpcclient.js",
  "scripts": {
    "test": "karma start --single-run --browsers PhantomJS test/unit.conf.js && && find coverage/ && cat coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm cat coverage/*/lcov.info"
  },
  "author": "Textalk AB http://textalk.se/",
  "license": "MIT",
  "devDependencies": {
    "karma": "~0.10.9",
    "karma-mocha": "~0.1.1",
    "karma-chai-sinon": "~0.1.1",
    "jquery": "~2.1.0",
    "karma-coverage": "~0.1.5",
    "coveralls": "~2.8.0",
    "mocha-lcov-reporter": "0.0.1"
  }
}
